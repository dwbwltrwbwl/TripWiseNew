@model TripWise.Models.UserVote

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>UserVote</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdVoteOption" class="control-label"></label>
                <select asp-for="IdVoteOption" class ="form-control" asp-items="ViewBag.IdVoteOption"></select>
            </div>
            <div class="form-group">
                <label asp-for="IdUser" class="control-label"></label>
                <select asp-for="IdUser" class ="form-control" asp-items="ViewBag.IdUser"></select>
            </div>
            <div class="form-group">
                <label asp-for="VotedAt" class="control-label"></label>
                <input asp-for="VotedAt" class="form-control" />
                <span asp-validation-for="VotedAt" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
